-- 1 create student table
CREATE TABLE STUDENT (
    SID NUMBER,
    SNAME VARCHAR2(30),
    AGE NUMBER,
    COURSE VARCHAR2(20)
);

-- 2 create employee table
CREATE TABLE EMPLOYEE (
    EID NUMBER,
    ENAME VARCHAR2(40),
    SALARY NUMBER(8,2),
    DEPTNO NUMBER
);

-- 3 create department table
CREATE TABLE DEPARTMENT (
    DEPTNO NUMBER,
    DNAME VARCHAR2(30),
    LOCATION VARCHAR2(30)
);

-- 4 create book table
CREATE TABLE BOOK (
    BOOK_ID NUMBER,
    TITLE VARCHAR2(50),
    AUTHOR VARCHAR2(40),
    PRICE NUMBER
);

-- 5 create course table
CREATE TABLE COURSE (
    CID NUMBER,
    CNAME VARCHAR2(40),
    DURATION NUMBER
);

-- 6 create project table
CREATE TABLE PROJECT (
    PID NUMBER,
    PNAME VARCHAR2(50),
    START_DATE DATE,
    END_DATE DATE
);

-- 7 create customer table
CREATE TABLE CUSTOMER (
    CID NUMBER,
    CNAME VARCHAR2(40),
    CITY VARCHAR2(30),
    PHONE NUMBER
);

-- 8 create sales table
CREATE TABLE SALES (
    SALE_ID NUMBER,
    PRODUCT_NAME VARCHAR2(50),
    QUANTITY NUMBER,
    PRICE NUMBER
);

-- 9 create emp_copy (structure only)
CREATE TABLE EMP_COPY AS
SELECT * FROM EMP WHERE 1=0;

-- 10 create emp_backup (with data)
CREATE TABLE EMP_BACKUP AS
SELECT * FROM EMP;

-- 11 add email to student
ALTER TABLE STUDENT ADD EMAIL VARCHAR2(50);

-- 12 add phone to employee
ALTER TABLE EMPLOYEE ADD PHONE NUMBER;

-- 13 modify sname size
ALTER TABLE STUDENT MODIFY SNAME VARCHAR2(50);

-- 14 modify salary datatype
ALTER TABLE EMPLOYEE MODIFY SALARY NUMBER(10,2);

-- 15 add city and pincode to customer
ALTER TABLE CUSTOMER 
ADD (CITY VARCHAR2(30), PINCODE NUMBER);

-- 16 rename sname to student_name
ALTER TABLE STUDENT RENAME COLUMN SNAME TO STUDENT_NAME;

-- 17 drop age column
ALTER TABLE STUDENT DROP COLUMN AGE;

-- 18 set salary not null
ALTER TABLE EMPLOYEE MODIFY SALARY NOT NULL;

-- 19 increase title size
ALTER TABLE BOOK MODIFY TITLE VARCHAR2(100);

-- 20 add manager_id
ALTER TABLE EMPLOYEE ADD MANAGER_ID NUMBER;

-- 21 drop student table
DROP TABLE STUDENT;

-- 22 drop customer table
DROP TABLE CUSTOMER;

-- 23 drop project permanently
DROP TABLE PROJECT PURGE;

-- 24 remove sales table
DROP TABLE SALES;

-- 25 drop course table
DROP TABLE COURSE;

-- 26 rename student to students
RENAME STUDENT TO STUDENTS;

-- 27 rename employee to emp_master
RENAME EMPLOYEE TO EMP_MASTER;

-- 28 rename customer to client
RENAME CUSTOMER TO CLIENT;

-- 29 rename book to book_details
RENAME BOOK TO BOOK_DETAILS;

-- 30 rename project to project_info
RENAME PROJECT TO PROJECT_INFO;

-- 31 remove all records from student
DELETE FROM STUDENT;

-- 32 truncate employee table
TRUNCATE TABLE EMPLOYEE;

-- 33 empty customer table
TRUNCATE TABLE CUSTOMER;

-- 34 remove all data from sales
TRUNCATE TABLE SALES;

-- 35 clear project table
TRUNCATE TABLE PROJECT;

-- 36 create training table
CREATE TABLE TRAINING (
    TID NUMBER,
    TNAME VARCHAR2(40),
    DURATION NUMBER
);

-- 37 add trainer_name column
ALTER TABLE TRAINING ADD TRAINER_NAME VARCHAR2(40);

-- 38 modify tname size
ALTER TABLE TRAINING MODIFY TNAME VARCHAR2(60);

-- 39 rename training to training_program
RENAME TRAINING TO TRAINING_PROGRAM;

-- 40 truncate training_program table
TRUNCATE TABLE TRAINING_PROGRAM;

-- 41 drop training_program table
DROP TABLE TRAINING_PROGRAM;

-- 42 create emp_project and modify
CREATE TABLE EMP_PROJECT (
    EMP_ID NUMBER,
    PROJECT_ID NUMBER,
    START_DATE DATE,
    END_DATE DATE,
    ROLE VARCHAR2(30)
);

ALTER TABLE EMP_PROJECT MODIFY ROLE VARCHAR2(50);
ALTER TABLE EMP_PROJECT ADD STATUS VARCHAR2(20);

-- 43 create product and modify
CREATE TABLE PRODUCT (
    PID NUMBER,
    PNAME VARCHAR2(50),
    PRICE NUMBER,
    CATEGORY VARCHAR2(30)
);

ALTER TABLE PRODUCT RENAME COLUMN PNAME TO PRODUCT_NAME;
ALTER TABLE PRODUCT MODIFY PRICE NUMBER(10,2);

-- 44 create orders and modify
CREATE TABLE ORDERS (
    ORDER_ID NUMBER,
    CUSTOMER_ID NUMBER,
    ORDER_DATE DATE,
    TOTAL_AMOUNT NUMBER
);

ALTER TABLE ORDERS ADD STATUS VARCHAR2(20);
ALTER TABLE ORDERS DROP COLUMN TOTAL_AMOUNT;
ALTER TABLE ORDERS ADD TOTAL_AMOUNT NUMBER(10,2);

-- 45 create emp_temp then rename, truncate, drop
CREATE TABLE EMP_TEMP AS
SELECT * FROM EMP WHERE SAL > 2000;

RENAME EMP_TEMP TO EMP_HIGH_SALARY;
TRUNCATE TABLE EMP_HIGH_SALARY;
DROP TABLE EMP_HIGH_SALARY;

-- 46 create student_record and modify
CREATE TABLE STUDENT_RECORD (
    SID NUMBER,
    SNAME VARCHAR2(40),
    COURSE VARCHAR2(30),
    MARKS NUMBER
);

ALTER TABLE STUDENT_RECORD ADD GRADE CHAR(1);
ALTER TABLE STUDENT_RECORD MODIFY MARKS NUMBER(5,2);
ALTER TABLE STUDENT_RECORD RENAME COLUMN COURSE TO COURSE_NAME;

-- 47 create emp_archive and modify
CREATE TABLE EMP_ARCHIVE AS
SELECT * FROM EMP WHERE 1=0;

ALTER TABLE EMP_ARCHIVE ADD ARCHIVE_DATE DATE;
RENAME EMP_ARCHIVE TO EMP_HISTORY;

-- 48 create dept_backup and modify
CREATE TABLE DEPT_BACKUP AS
SELECT * FROM DEPT;

ALTER TABLE DEPT_BACKUP DROP COLUMN LOC;
ALTER TABLE DEPT_BACKUP ADD LOCATION VARCHAR2(40);

-- 49 create sales_data and modify
CREATE TABLE SALES_DATA (
    SALE_ID NUMBER,
    PRODUCT_ID NUMBER,
    SALE_DATE DATE,
    AMOUNT NUMBER
);

ALTER TABLE SALES_DATA ADD REGION VARCHAR2(30);
ALTER TABLE SALES_DATA MODIFY AMOUNT NUMBER(12,2);
RENAME SALES_DATA TO REGIONAL_SALES_DATA;

-- 50 create log_table and modify
CREATE TABLE LOG_TABLE (
    LOG_ID NUMBER,
    USER_NAME VARCHAR2(30),
    ACTION VARCHAR2(50),
    LOG_DATE DATE
);

TRUNCATE TABLE LOG_TABLE;
RENAME LOG_TABLE TO SYSTEM_LOG;
DROP TABLE SYSTEM_LOG;

INSERT INTO BOOK (
    BOOK_ID,
    TITLE,
    AUTHOR,
    PRICE
) VALUES ( :BOOK_ID,
           :TITLE,
           :AUTHOR,
           :PRICE );

DELETE FROM BOOK
WHERE
        BOOK_ID = :BOOK_ID
    AND TITLE = :TITLE
    AND AUTHOR = :AUTHOR
    AND PRICE = :PRICE;



DROP TABLE STUDENTS;
DROP TABLE EMP_MASTER;
DROP TABLE DEPARTMENT;
DROP TABLE BOOK_DETAILS;
DROP TABLE CLIENT;
DROP TABLE EMP_COPY;
DROP TABLE EMP_BACKUP;
DROP TABLE EMP_PROJECT;
DROP TABLE PRODUCT;
DROP TABLE ORDERS;
DROP TABLE STUDENT_RECORD;
DROP TABLE EMP_HISTORY;
DROP TABLE DEPT_BACKUP;
DROP TABLE REGIONAL_SALES_DATA;
DROP TABLE EMP_AUDIT;
drop table dept_copy;